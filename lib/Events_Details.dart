
class EventDetails{
  String title,subTitle,desc,imageURL;

  getDetails(title,subTitle,desc,imageURL){
    this.title=title;
    this.subTitle = subTitle;
    this.desc = desc;
    this.imageURL = imageURL;
  }
}